version: '3.8'

services:
  zx11408-wiki:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: zx_11408_docs
    restart: always
    ports:
      - "985:80"  # 您可以通过 服务器IP:985 访问
    networks:
      - 1panel-network

networks:
  1panel-network:
    external: true
